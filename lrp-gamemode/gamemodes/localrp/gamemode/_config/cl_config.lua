lrp_cfg = lrp_cfg or {}

local cfg = lrp_cfg

cfg.pushHintBlacklist = {
    weapon_physgun = true,
    gmod_tool = true,
    gmod_camera = true,
}

cfg.colors = {
    main = Color(0, 80, 65),
    second = Color(5, 60, 50),
    defbtn = Color(0, 125, 100),
    hovbtn = Color(0, 185, 150),
}