lrp_cfg = lrp_cfg or {}

local cfg = lrp_cfg

cfg.defaultWeapons = {
    localrp_hands = true,
    weapon_physgun = true,
    gmod_tool = true,
    gmod_camera = true,
}

cfg.sboxMenuWhiteList = {
    weapon_physgun = true,
    gmod_tool = true,
}

cfg.pushDist = 70